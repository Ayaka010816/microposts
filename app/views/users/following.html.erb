<div class="row">
  <div class="col-md-4">
   <h2>following</h2>
 <ul>
  <li>
  <% @following.each do |u| %>
  <%= link_to gravatar_for(u, size: 50), u %>
  <span class="user"><%= link_to u.name, u %></span>
  <% end %>
  </li>
 </ul>
  </div>
</div>  